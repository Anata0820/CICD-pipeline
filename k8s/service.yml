apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: awesome-ns
  labels:
    app: awesome-app
spec:
  selector:
    app: awesome-app
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: 80